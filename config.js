/* Локальный конфиг — НЕ коммитить в git.
   До настройки Apps Script сайт работает в "demo"-режиме: телефоны падают в localStorage. */
window.CONFIG = {
  APPS_SCRIPT_URL: '',           // пусто = демо-режим, см. apps-script/README.md
  TERMINAL_ID: 'atm-001',
};
